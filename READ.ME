# Match Up
* Backend: Spring Boot (Java)
* Database: MongoDB (NoSQL, great for flexible data models)
* API: GraphQL (Spring for GraphQL)
* Authentication: Firebase Auth (JWT-based security)

To set up your development environment 
1. Clone this repo
2. Build image locally: docker build -t matchup-backend-image .
3. Start the service by docker-compose up -d
